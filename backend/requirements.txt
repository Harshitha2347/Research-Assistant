--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# LlamaIndex core + integrations
langchain-core==0.2.43
langsmith==0.1.147
llama-index-core==0.11.14
llama-index-vector-stores-qdrant==0.3.0
llama-index-retrievers-bm25==0.3.0
llama-index-postprocessor-flag-embedding-reranker==0.2.0

# Vector DB
qdrant-client==1.11.2

# Reranker (lightweight, local, no API — only the embedding step was
# moved to the hosted Gemini API)
FlagEmbedding==1.2.11

# PDF + image extraction
pymupdf==1.24.10
Pillow==10.4.0

# LLM providers
groq==0.11.0
google-generativeai==0.8.2
tavily-python==0.5.0

# Supabase
supabase==2.7.4
PyJWT==2.13.0

# Evaluation (imported lazily inside evaluation.py, only when /evaluation/run
# is actually called, so these don't add to the baseline memory footprint
# of every other request, including document upload)
ragas==0.1.21
datasets==3.0.1
langchain-groq==0.1.9

# Misc
numpy==1.26.4
python-dotenv==1.0.1
rank-bm25==0.2.2
tiktoken==0.7.0
peft